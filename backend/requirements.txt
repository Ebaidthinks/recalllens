# Core Framework
fastapi
uvicorn[standard]
python-multipart

# Data Processing
numpy
Pillow

# Computer Vision & ML
# Note: PyTorch CPU-only version installed separately in Dockerfile for free tier optimization
# torch==2.1.0 (installed via CPU wheel)
# torchvision==0.16.0 (installed via CPU wheel)
opencv-python-headless  # Headless version (no GUI deps) for Docker
pytorch-grad-cam  # Installed after PyTorch in Dockerfile

# OCR & Text Processing
paddleocr
transformers

# Scientific Computing
scikit-image
scipy

# Report Generation
reportlab

# Configuration
pydantic
pydantic-settings

# Video Processing
ffmpeg-python
